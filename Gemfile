source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.6'

# Use 'mongoid' and 'bson_ext' for Mongodb.
gem 'mongoid', git: 'git://github.com/mongoid/mongoid.git'
gem 'bson_ext'
# Use mongoid-slug for slug support in models.
gem 'mongoid-slug'

# Use Devise for user handling
gem 'devise'

# Use Angular for Front End Javascript framework
gem 'angularjs-rails'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'

# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Use 'devise' for authentication
gem 'devise'

# Use elasticsearch for full-text search on Rails objects
gem 'elasticsearch-ruby'
gem 'mongoid-elasticsearch'


# Use 'font-awesome-rails' for awesome fonts
gem 'font-awesome-rails'
# Use 'font-kit-rails' for a pack of text fonts.
gem 'font-kit-rails'

# Use 'carrierwave-mongid' 'mongoid-grid_fs' for file uploading
gem 'carrierwave-mongoid'
gem 'mongoid-grid_fs', github: 'ahoward/mongoid-grid_fs'
gem 'carrierwave-base64'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
gem 'unicorn'

# Use 'enumerize' for enum support
gem 'enumerize'

# Use 'kaminari' for pagination
gem 'kaminari'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'

  # Use Pry for a better rails console
  gem 'pry-rails'

  # Use RSpec for software specification (tests)
  gem 'rspec-rails'
  # Use FactoryGirl for factories.
	gem 'factory_girl_rails'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
end

